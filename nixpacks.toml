[phases.setup]
nixPkgs = ["php81", "php81Extensions.pdo", "php81Extensions.sqlite3", "php81Extensions.curl", "php81Extensions.mbstring", "php81Extensions.openssl", "php81Extensions.json"]

[phases.build]
cmds = ["echo 'PHP project - no build step required'"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t ."
